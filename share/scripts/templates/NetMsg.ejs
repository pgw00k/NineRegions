// 由 mc-local-share generate_ts 自动生成，请勿手改。
// tagName: <%- TAG %>

import { MessageBase } from '../MessageBase';
import {
  MESSAGE_ID,
<% if (REQ_TYPE !== '{}') { %>  <%- REQ_TYPE %>,
<% } %><% if (RES_TYPE !== '{}') { %>  <%- RES_TYPE %>,
<% } %>} from 'mc-local-share';

/**
 * <%- TAG %>
 * REQ = <%- REQ_TYPE %>
 * RES = <%- RES_TYPE %>
 * 注册：reqId=<%- REQ_ID_NUM %>,recId=<%- REC_ID_NUM %>
 */
export class <%- CLASS_NAME %> extends MessageBase<<%- REQ_TYPE %>, <%- RES_TYPE %>> {
  /** 请求消息号：<%- REQ_ID_NAME %> (<%- REQ_ID_NUM %>) */
  reqId: MESSAGE_ID = MESSAGE_ID.<%- REQ_ID_NAME %>;
  /** 响应消息号：<%- REC_ID_NAME %> (<%- REC_ID_NUM %>) */
  recId: MESSAGE_ID = MESSAGE_ID.<%- REC_ID_NAME %>;

  override Handle(req: <%- REQ_TYPE %>): <%- RES_TYPE %> {
    let resobj = super.Handle(req)
    if(!resobj) {
      throw new Error('Handle not implemented: <%- TAG %>');
    }
    return resobj
  }
}